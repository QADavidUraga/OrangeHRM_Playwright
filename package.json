{
  "name": "playwright-orangehrm",
  "version": "2.0.0",
  "description": "OrangeHRM end-to-end tests using Playwright and TypeScript. Chatbot tests included.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:orangehrm": "playwright test orangehrm.spec.ts",
    "test:chatbot": "playwright test chatbot.spec.ts",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
